/**
 * 内置规则索引文件
 * 统一导出所有内置规则，供前端设置页面使用
 */

export { DEFAULT_REGEX_RULES, type RegexRule, type RegexRulesConfig } from './builtin-regex-rules'
export { DEFAULT_AST_RULES, type AstRule, type AstRulesConfig } from './builtin-ast-rules'